[plugin]
name = "fledge-plugin-jwt"
version = "0.1.0"
description = "Decode and inspect JWTs from the CLI or a beautiful web UI"
author = "CorvidLabs"
license = "MIT"

[[commands]]
name = "jwt"
description = "JWT inspector and decoder"
binary = "bin/jwt"

[capabilities]
exec = true
store = false
metadata = false

